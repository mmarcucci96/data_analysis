# Le voy a asignar una variable a un pedezo de mi data. El slash se usa para traer la data.

file_name <- 'data/quijote.txt'

# Import a plain text file with readLines function
readLines(file_name, n=10)

# To change languages in R (tildes) with encoding function
readLines(file_name, n=10, encoding='UTF-8')

# Avoid loading NULL values
readLines(file_name, n=10, skipNul = TRUE, encoding='UTF-8')


# A package that enables reading in R. Tools like readLines
install.packages('readr')
library(readr)

# Import plain text using readr (another option)
quijote <- read_lines(file= file_name)

# Indexing example with one or more vectors
t <- c(20,22,18,19)
t
t[2]

t[c(2,3)]
t[2:3]

#Indexing example by condition, how to exclude NA (Boolean and position)
a <- c(2,24,25,3,87,NA,63,27)
a[-c(6)]
a[c(-6)]
mean(a[c(-6)])

#Boolean. How to remove NA from my vector. AKA Filtrar.
!is.na(a)
a[!is.na(a)]

# Excercise. Show all the values in which a is higher or equal than 25. Hopw many? 5 and to delete NA is 4, below
a>=25
length(a[a>=25])
a[c(!is.na(a) & a>=25)]

